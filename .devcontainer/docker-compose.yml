version: '3.8'
services:
  iris:
    build: 
      context: .
      dockerfile: Dockerfile
    user: root  # Forzamos root para evitar el error 1302 en el despliegue inicial
    privileged: true 
    ports:
      - 52773:52773
      - 1972:1972
    volumes:
      - .:/home/vscode/workspace
